# Copy to backend.hcl (gitignored) and run: terraform init -backend-config=backend.hcl
# Bucket + lock table are created by ../../bootstrap
bucket         = "jaider012-tfstate"
key            = "week2-vpc-lab/terraform.tfstate"
region         = "us-east-1"
dynamodb_table = "terraform-locks"
encrypt        = true
